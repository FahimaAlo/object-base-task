const newProperty = "MY exam is completed .Our eXercise";

// Replace both uppercase 'X' and lowercase 'x' with 'Y' and 'y' respectively
const updatedString = newProperty.replace(/X/g, 'Y').replace(/x/g, 'y');

console.log(updatedString); 
